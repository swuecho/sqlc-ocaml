version: "2"
plugins:
  - name: ocaml
    process:
      cmd: /src/bin/sqlc-gen-ocaml-linux
      format: json
sql:
  - engine: postgresql
    schema: schema.sql
    queries: queries.sql
    codegen:
      - plugin: ocaml
        out: generated
        options:
          filename: queries
